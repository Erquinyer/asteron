PORT=3000

# Base de datos MySQL — pon la contraseña de tu usuario root (vacío si no tiene contraseña)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=asteron

# JWT — puedes dejar este valor o cambiarlo por una cadena segura propia
JWT_SECRET=asteron_super_secret_2026
JWT_EXPIRES_IN=8h

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:5173

# Correo para recuperación de contraseña (opcional)
EMAIL_USER=
EMAIL_PASS=
